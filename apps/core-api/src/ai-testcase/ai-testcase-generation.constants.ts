/** Ngưỡng ký tự statement — đồng bộ với frontend cảnh báo. */
export const LONG_STATEMENT_THRESHOLD = 4000;

export const STATEMENT_WARN_THRESHOLD = 4000;

/** Fast mode chỉ khi statement ngắn và ít case (AI_FAST_MODE=auto). */
export const FAST_MODE_AUTO_MAX_STATEMENT = 2500;

export const FAST_MODE_AUTO_MAX_CASES = 6;
